inc lib/stdlib.slm
inc lib/tables.slm

gvar table 4096

proc main 0 0
  table 8 4096 (tab.new)

  "lol" 32
  (tab.put)

  "=======" (cstr.println)
  copy (tab.print)
  "=======" (cstr.println)
  
  "lmao" 69
  (tab.put)

  copy (tab.print)
  "=======" (cstr.println)
  
  "lmao" 42
  (tab.put)

  copy (tab.print)
  "=======" (cstr.println)

  "lmao" (tab.in)
  (cstr.printint)

  "lmao" (tab.get)
  (cstr.printint)
  (tab.print)

  ret
end

